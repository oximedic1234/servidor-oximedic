require('dotenv').config(); const { Pool } = require('pg'); const pool1 = new Pool({ connectionString: 'postgresql://postgres:OXIMEDIC2026@db.fhgoutlylsjpcukawzmn.supabase.co:5432/postgres', ssl: { rejectUnauthorized: false } }); const pool2 = new Pool({ connectionString: 'postgresql://postgres:yZrd1w3pUofVwW4Z@db.uocldwukdcdownddoxen.supabase.co:5432/postgres', ssl: { rejectUnauthorized: false } }); async function run() { try { console.log('Altering Oximedic DB...'); await pool1.query('ALTER TABLE Clientes ADD COLUMN IF NOT EXISTS Direccion_Domicilio TEXT;'); await pool1.query('ALTER TABLE Recargas ADD COLUMN IF NOT EXISTS Es_Domicilio BOOLEAN DEFAULT FALSE;'); await pool1.query('ALTER TABLE Recargas ADD COLUMN IF NOT EXISTS Costo_Transporte NUMERIC(10,2) DEFAULT 0.0;'); console.log('Oximedic OK'); console.log('Altering Oxicenter DB...'); await pool2.query('ALTER TABLE Clientes ADD COLUMN IF NOT EXISTS Direccion_Domicilio TEXT;'); await pool2.query('ALTER TABLE Recargas ADD COLUMN IF NOT EXISTS Es_Domicilio BOOLEAN DEFAULT FALSE;'); await pool2.query('ALTER TABLE Recargas ADD COLUMN IF NOT EXISTS Costo_Transporte NUMERIC(10,2) DEFAULT 0.0;'); console.log('Oxicenter OK'); } catch (e) { console.error(e); } finally { await pool1.end(); await pool2.end(); } } run();
